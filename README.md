### Hackernews GraphQL Clone

Exploring GraphQL by building a Hackernews clone.

**Server**
A simple NodeJS server using `express`, `graphql-yoga`, and `prisma`.

**Client**
A React app mainly using `apollo-client`.